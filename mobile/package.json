{
  "name": "health-chn-app",
  "version": "1.0.0",
  "description": "悦动健康 App（Capacitor 打包）",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync android && cd android && gradlew assembleDebug",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}